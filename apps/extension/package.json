{
  "name": "@rewards-audit/extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "tsc -p tsconfig.json --noEmit && vite build && node scripts/copy-assets.mjs",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "jsdom": "27.3.0",
    "vite": "5.4.21",
    "vitest": "2.1.8"
  }
}
